import { defineConfig } from 'vite';

export default defineConfig({
  // Set to the GitHub repository name so assets resolve correctly on GitHub Pages.
  // Example: if the repo is github.com/you/sign-of-life, use '/sign-of-life/'
  base: '/sign-of-life/',
});
